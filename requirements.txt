numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
yfinance>=0.2.0
lxml>=4.9.0
torch>=2.0.0
pytest>=7.0.0
matplotlib>=3.7.0
# Optional: WRDS access for the JKP panel (Jensen-Kelly-Pedersen 153 characteristics)
# + CRSP daily returns. Required only when using cmm.fetch_data_jkp.
wrds>=3.2.0
